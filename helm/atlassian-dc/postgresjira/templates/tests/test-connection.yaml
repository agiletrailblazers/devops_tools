apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postgresjira.fullname" . }}-test-connection"
  labels:
    {{- include "postgresjira.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "postgresjira.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
